import 'package:flutter_test/flutter_test.dart';

void main() {
  group('SupabaseServiceRepository', () {
    // late ProviderContainer container;

    // setUp(() {
    //   container = ProviderContainer(
    //     overrides: [
    //       serviceRepositoryProvider.overrideWithValue(MockServiceRepository()),
    //     ],
    //   );
    // });

    // test(
    //     'getServices returns empty list when supabase returns null response',
    //     () async {
    //   // Arrange
    //   final repository = container.read(serviceRepositoryProvider);

    //   // Act
    //   final result = await repository.getServices();

    //   // Assert
    //   expect(result, isA<List<Service>>());
    //   expect(result, isEmpty);
    // });
  });
}
